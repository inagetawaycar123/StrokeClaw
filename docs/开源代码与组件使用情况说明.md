# StrokeClaw 项目开源代码与组件使用情况说明

## 1. 结论
- 该项目**已使用开源代码与开源组件**，包括：
- Python 生态开源依赖（AI/医学影像/服务框架等）。
- 前端开源依赖（React/Vite 生态）。
- 构建产物中打包的开源运行时代码（React MIT 许可声明可见）。
- 源码中存在明确标注来源的开源实现借鉴/移植片段。
- 报告生成通过百川 M3 API 完成，不再分发本地报告大模型资产。

## 2. 检查范围与方法
- 扫描依赖清单：`pyproject.toml`、`uv.lock`、`requirements*.txt`、`frontend/package.json`、`frontend/package-lock.json`。
- 检索源码中的来源与许可线索：`license`、`github.com`、`ported from`、`borrowed` 等关键词。
- 复核构建产物中是否包含第三方开源代码许可头。

## 3. 已识别的开源组件与代码

### 3.1 Python 开源依赖（项目声明）
证据：
- `pyproject.toml:7-30` 声明了主要依赖（节选）：
  - `torch`、`torchvision`
  - `flask`、`python-dotenv`
  - `numpy`、`scipy`、`scikit-image`、`opencv-python`
  - `nibabel`、`pandas`、`matplotlib`
  - `supabase`、`tqdm`、`einops` 等
- `uv.lock:793-819`（项目包 `neuro-latest`）再次锁定上述依赖。
- `uv.lock` 中共检出 `[[package]]` 条目约 **124** 个（含传递依赖）。

结论：
- 后端与算法模块明确依赖大量开源 Python 组件。

### 3.2 前端开源依赖（项目声明）
证据：
- `frontend/package.json:11-17`
  - 运行时依赖：`react`、`react-dom`
  - 开发依赖：`vite`、`@vitejs/plugin-react`
- `frontend/package-lock.json:10-17` 与其一致，并锁定传递依赖。

结论：
- 前端基于 React + Vite 开源生态构建。

### 3.3 源码中明确标注来源的开源实现（代码级）
证据：
- `palette/core/base_network.py:14`
  - 注释直接指向 `junyanz/pytorch-CycleGAN-and-pix2pix` 对应实现链接。
- `palette/models/guided_diffusion_modules/unet.py:208-209`
  - 注释写明 “Originally ported from here...”
  - 指向 `hojonathanho/diffusion` 的 U-Net 实现链接。

结论：
- 项目中存在“代码实现层面的开源借鉴/移植”，不仅是包依赖调用。

### 3.4 构建产物中打包的开源代码
证据：
- `frontend/dist/assets/index-BVen66sD.js:2-8`
  - 包含 React 的 `@license` 与 MIT 许可说明。

结论：
- 发布产物内包含第三方开源代码片段（符合常见前端打包形态）。

## 4. 当前合规状态评估
- 已明确存在开源使用事实（依赖 + 代码移植 + 打包代码 + 模型条款）。
- 仓库根目录未发现统一的项目 `LICENSE` 文件（未检出顶层 `LICENSE*`）。
- 目前缺少集中式第三方组件及许可证归档（如 `THIRD_PARTY_NOTICES` / SBOM）。

## 5. 建议（用于比赛/交付材料）
- 新增 `THIRD_PARTY_NOTICES.md`：列出直接依赖、关键传递依赖、许可证类型、来源链接。
- 将代码级借鉴条目单列为“开源实现参考说明”（含上述 2 处源码链接证据）。
- 对百川 M3 API 的服务条款、数据处理和调用边界单独归档。
- 在仓库根目录补充项目自身 `LICENSE`（若决定开源发布）与版权声明。

